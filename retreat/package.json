{
  "name": "ai-compass-retreat",
  "private": true,
  "version": "1.0.0",
  "description": "Shared-board version of the AI Compass for a small in-person activity",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && npm run test:static && wrangler deploy",
    "test": "npm run build && npm run test:static && npm run test:e2e",
    "test:static": "node test/static.mjs",
    "test:e2e": "node test/e2e.mjs",
    "rooms": "node tools/rooms.mjs",
    "migrate": "wrangler d1 migrations apply ai-compass-retreat --remote",
    "migrate:local": "wrangler d1 migrations apply ai-compass-retreat --local"
  }
}
